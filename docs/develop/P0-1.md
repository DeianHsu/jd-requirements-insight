# P0-1 抽取数据合同与人工标注规范｜Develop

```yaml
p0_id: "P0-1"
develop_status: "completed"
task_base_commit: "historical; normalized at bdd8932"
implementation_revision: "bdd8932"
updated_at: "2026-08-02"
```

## 当前实现状态

抽取数据合同V2、人工标注规范1.4、SQLite持久化与旧库增量迁移均已实现，当前作为下游功能的稳定上游合同使用。

## 实际实现

- Pydantic Schema实现职责、原子要求、任选逻辑组、经验区间和证据字段校验。
- SQLAlchemy模型保存JD、抽取版本、职责、要求实例及其证据，数据库启动时执行可重复迁移。
- 标注规范拆分为职责、要求与数据集评测主题，并补充示例/点名技术等边界。

## 修改文件

- `app/schemas.py`
- `app/models.py`
- `app/database.py`
- `docs/annotation/`
- `docs/GLOSSARY.md`
- `tests/test_schemas.py`、`tests/test_database.py`及相关持久化测试

## 当前测试结果

- 当前仓库已有全套111项测试通过及Ruff通过的验证记录；覆盖Schema校验、旧字段兼容和数据库迁移。

## 当前实验摘要

- 10个开发集样例已按规范审核；原5个验证集职责样例因参与Prompt设计转为回归集。

## 与Design的当前偏差

- 无。

## 当前问题与遗留事项

- 新的未见验证集由P0-3/P0-5维护，不影响本功能合同验收。

## Developer建议

- 下游如需新增字段或改变语义，应先提交Change Request并重新验收P0-1。

## 未解决Change Request

- 无。

## 当前报告路径

- 无；规范与代码本身是权威材料。

