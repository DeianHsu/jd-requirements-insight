# P0-2 JD结构化抽取｜Develop

```yaml
p0_id: "P0-2"
develop_status: "completed"
task_base_commit: "historical; normalized at bdd8932"
implementation_revision: "bdd8932"
updated_at: "2026-08-02"
```

## 当前实现状态

结构化抽取客户端、有限重试、证据存在性校验、版本隔离和持久化流程均已实现；Prompt V2.3.1是当前正式抽取版本。

## 实际实现

- 客户端使用`temperature=0`和JSON Object模式请求模型，并按P0-1 Schema解析。
- 结构、证据或响应错误进入有限重试；最终失败返回有限错误信息。
- 抽取记录保存模型、Prompt和Schema版本，不同抽取器版本可共存。
- CLI可选择已导入JD执行抽取并持久化成功结果。

## 修改文件

- `app/extraction.py`
- `app/services.py`
- `app/cli.py`
- `app/config.py`
- `tests/test_extraction.py`及相关CLI、服务和持久化测试

## 当前测试结果

- 当前仓库已有全套111项测试通过及Ruff通过的验证记录；抽取重试、证据校验和版本隔离路径已有测试。

## 当前实验摘要

- DeepSeek V4 Flash使用Prompt V2.3.1与Schema V2对5份真实JD完成重抽取，最终失败0份。

## 与Design的当前偏差

- 无。

## 当前问题与遗留事项

- 原子粒度和未见集质量属于P0-3/P0-5，不影响P0-2结构化抽取能力验收。

## Developer建议

- 后续Prompt候选只有在独立验收通过后才替换V2.3.1正式版本。

## 未解决Change Request

- 无。

## 当前报告路径

- Prompt分析材料按其评测归属迁入`reports/P0-3/`。

