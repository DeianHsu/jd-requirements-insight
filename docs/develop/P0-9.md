# P0-9 核心自动化测试｜Develop

```yaml
p0_id: "P0-9"
develop_status: "in_progress"
task_base_commit: "historical; normalized at bdd8932"
implementation_revision: "bdd8932"
updated_at: "2026-08-02"
```

## 当前实现状态

当前仓库共有111项测试并全部通过，Ruff通过；导入、抽取、Schema、迁移、两段式、归并批次和关系合同等已覆盖。P0-6、P0-7和P0-10尚未实现，对应专项测试仍缺失。

## 实际实现

- 测试使用临时目录、虚构JD和临时SQLite数据库，隔离真实项目数据。
- P0-3覆盖中间合同、唯一覆盖、证据、重试和实验脚本付费保护。
- P0-4覆盖范围完整性、输入指纹、幂等、旧库迁移、外键、调用层重试、关系假阳性/冲突和显式批次评测。
- 抽取、导入、配置、服务和CLI的核心失败路径具有确定性测试。

## 修改文件

- `tests/`全部当前测试文件
- `pyproject.toml`中的测试与Ruff配置

## 当前测试结果

- `pytest`：111 passed。
- `ruff check .`：通过。
- 存在1个来自FastAPI TestClient依赖链的Starlette/httpx弃用警告，不影响当前行为。

## 当前实验摘要

- 无；LLM语义指标由对应P0实验与人工标准答案评测维护。

## 与Design的当前偏差

- P0-6独立JD计数、P0-7证据端到端查询和P0-10报告生成测试尚未建立。

## 当前问题与遗留事项

- 下游功能未实现，当前无法提前编写其最终合同测试。
- 依赖链弃用警告可在兼容性升级时处理，不阻塞当前P0。

## Developer建议

- 每个下游P0开发时同步补目标测试；完成P0-10后再运行完整门禁并申请P0-9 Review。

## 未解决Change Request

- 无。

## 当前报告路径

- 无；测试输出摘要保留在本文件，必要的完整日志放入对应P0报告目录。
