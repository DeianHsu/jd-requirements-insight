# P0-8 扩充至15～20份真实JD｜Develop

```yaml
p0_id: "P0-8"
develop_status: "in_progress"
task_base_commit: "historical; normalized at bdd8932"
implementation_revision: "bdd8932"
updated_at: "2026-08-02"
```

## 当前实现状态

Markdown解析、Front Matter校验、SHA-256正文哈希去重、逐文件事务导入和错误隔离已实现；私有真实数据当前为5份，尚未达到15～20份验收目标。

## 实际实现

- 解析JD Markdown和来源元数据，规范化正文后计算哈希。
- 已存在正文哈希时幂等跳过；单文件失败记录错误并继续处理其他文件。
- CLI输出有限摘要，测试使用临时虚构文件和数据库。

## 修改文件

- `app/ingestion.py`
- `app/cli.py`
- `tests/test_ingestion.py`、`tests/test_cli.py`

## 当前测试结果

- 当前仓库已有全套111项测试与Ruff通过记录；导入测试覆盖元数据解析、空白规范化、幂等和文件级错误隔离。

## 当前实验摘要

- 本地真实JD共5份；详细正文和数据库均位于Git忽略的私有位置。

## 与Design的当前偏差

- 样本数量不足，来源与筛选记录尚未形成完整闭环。

## 当前问题与遗留事项

- 还需选择并导入10～15份符合范围的真实JD。
- 用于完整归并前，全部目标JD必须完成同一正式抽取器版本。

## Developer建议

- P0-8可与P0-3、P0-4并行推进；先补数据来源与筛选摘要，再分批导入和抽取，避免一次性排查大量私有文件。

## 未解决Change Request

- 无。

## 当前报告路径

- 无公开详细报告；真实数据范围只在私有环境记录，Develop保留数量和结论。
