# P0-3 原子要求粒度验证与改进

```yaml
p0_id: "P0-3"
plan_item: "原子要求粒度验证与改进"
status: "partial"
baseline_commit: "78facdd"
verified_revision: "working tree based on 78facdd"
related_decisions: ["DEC-008", "DEC-016", "DEC-017"]
glossary_terms: ["原子要求", "固定复合要求", "困难样例", "名称代理指标"]
depends_on: ["P0-1", "P0-2", "P0-5"]
affects: ["P0-4", "P0-8", "P0-11"]
```

# 功能目标与边界

验证并改进P0-2产出的职责和原子要求粒度，确保复合条件不漏拆、不多拆，并保留正确证据与逻辑组。P0-3不定义新的要求类型，也不执行跨JD要求归并。

# 当前状态

部分完成（恢复开发中）。两段式 v0.6 判断段规则补齐 1 轮后，全量5份JD（0失败）达成全部候选门槛：开发集要求名称F1 100%、职责F1 100%、数量一致10/10、any_of 7/7、类别90.91%、熟练度90.91%、证据存在率48/48；回归集职责F1 100%、数量一致5/5（V2.3.1基线为回归职责78.26%/2/5，两段式历史最佳92.31%/3/5）。V2.3.1仍为正式Prompt，两段式v0.6为待正式验收实验版本；详见`reports/P0_3_TWO_STAGE_EXPERIMENT.md`第7节与`reports/P0_3_ROOT_CAUSE_ANALYSIS.md`。下一步：建立未见验证集（P0-5规则）后执行正式验收。

# 稳定事实

- 原子化以能否独立学习、分类、评价、匹配或统计为判断基础。
- 固定复合要求或拆分后失真的表达整体保留。
- 非穷举示例不建立封闭`any_of`；被候选条件直接修饰的具体技术名逐项保留。
- 职责先识别独立交付，再判断多个动作是否属于同一职责。
- 开发集和回归集已经参与设计，不能宣称为新的未见验证集。
- 正式抽取仍使用V2.3.1；两段式v0.6已在开发集/回归集达成全部候选门槛（要求侧追平、职责侧超越），但尚未在未见验证集正式验收，除非新验证集达标否则不得替换正式Prompt。
- 两段式发现合同必须先证明全文每个分句恰好归属一个候选块，再进入判断段；覆盖检查失败不得通过Prompt结果“容错”或后处理静默修正。

# 实现与文件入口

- `app/extraction.py`：当前Prompt和实验入口。
- `app/extraction_two_stage.py`：两段式实验模块（发现段/判断段、中间合同、覆盖检查；已归档，不接入正式抽取）。
- `scripts/experiments/p0_3/run_two_stage_extraction.py`：显式选择数据库并确认付费调用的真实两段式实验入口。
- `scripts/experiments/p0_3/evaluate_two_stage_results.py`：读取私有结果并生成离线分层指标报告。
- `tests/test_extraction_two_stage.py`：两段式合同、唯一覆盖与重试测试（12项）。
- `docs/annotation/REQUIREMENTS.md`、`RESPONSIBILITIES.md`：粒度规则。
- `tests/test_extraction.py`：Prompt边界和实验测试。
- `reports/PROMPT_V2_ANALYSIS.md`至`PROMPT_V2_3_1_ANALYSIS.md`：历史实验；`reports/P0_3_TWO_STAGE_EXPERIMENT.md`：两段式归档。
- `docs/DEEPSEEK_CONTINUATION.md`：跨模型冷启动时使用的Codex审计差异、建议恢复顺序和提示词；P0-3状态仍以本handoff为准。

# 数据合同与不变量

- 原子化结果仍使用P0-1的`RequirementItem`和`ResponsibilityItem`。
- 多个原子项可以共享同一连续证据，但不得拼接不连续文本。
- 不按顿号、“和”或“与”机械拆分。
- 不因模型当前输出修改人工标准答案。
- 两段式发现结果的块ID唯一，分句索引非负、有序且连续；全文每个分句必须且只能归属一个块，原文片段必须对应声明索引。

# 测试与验证

当前工作树全量测试和Ruff通过；两段式实验12项专项测试覆盖重复分句、重复块ID、负索引、非连续合并、片段对应和重试，实验脚本测试覆盖私有输出边界、显式`--execute`和`--job-id`范围。v0.6全量5份真实JD（10次调用，0失败）已重跑并达到全部候选门槛（见"当前状态"），业务质量结论以2026-08-01恢复轮的实测指标为准。

# 未完成事项与已知问题

- 根因已定位（只读诊断，2026-08-01，`reports/P0_3_ROOT_CAUSE_ANALYSIS.md`）：瓶颈在判断段不在发现段。要求侧 82.93% 的漏项来自 case_001/case_010 的有限任选 any_of 组被压缩为上位概念（DEC-017 规则未继承，8 漏项）+ case_002 名称代理匹配偏差（模型实际正确，属 P0-5 评测口径，不得为刷指标改评测）；职责侧残余过合集中在 case_005/011/012（DEC-016 未完整继承）。恢复时只补判断段规则 1-2 轮，不改发现段。
- 熟练度准确率90.91%、类别准确率90.91%（均达到候选门槛），残留分歧：case_002"理解"未映射understand、case_002/004/007少量category判断分歧，不影响门槛，是否再补1轮规则由恢复节奏决定。
- 两段式v0.6要求侧已追平V2.3.1（开发集要求名称F1 100%），职责侧超越（回归集职责F1 100%）。
- 新验证集待创建（当前开发集/回归集已参与设计）。
- v0.6已达全部候选门槛（开发集要求名称F1与any_of 100%、类别≥90.91%、熟练度≥86.36%、回归集职责F1≥92.31%、数量一致≥3/5、证据存在率100%）；下一步冻结未见验证集后执行正式验收，验证 temperature=0 输出稳定性（历史实验同一版本重跑有波动）。
- 正式接入还需要P0-5按规则建立未见验证集，并记录正式Prompt切换与回滚边界。

# 继续开发入口

先读取本handoff、`reports/P0_3_TWO_STAGE_EXPERIMENT.md`、`reports/P0_3_ROOT_CAUSE_ANALYSIS.md`、两个粒度规范、`app/extraction_two_stage.py`和`app/extraction.py`中V2.3.1的Prompt规则；跨模型接手再读取`docs/DEEPSEEK_CONTINUATION.md`。

当前阶段（2026-08-01恢复轮）：v0.6已达全部候选门槛，剩余流程：

1. 人工标注并批准冻结未见验证集（候选清单见`data/private/experiments/p0_3/validation_candidates.md`，10条，与开发/回归集无重叠）；一旦用验证集数据调参，它即转为开发集或回归集，必须另建未见样例。
2. 验证集冻结后执行正式验收（独立运行全量5份JD，验证temperature=0输出稳定性），门槛：开发集要求名称F1与`any_of`、证据存在率100%，类别≥90.91%，熟练度≥86.36%，回归集职责F1≥92.31%，数量一致≥3/5，新验证集端到端原子要求F1≥70%，发现段合同全过；正式验收按AGENTS.md规则计次并记录版本、范围、指标、原因与连续失败次数。
3. 新验证集、全量测试和Ruff全部通过后，才评审是否把两段式接入正式抽取并同步下游，明确切换与回滚边界；否则保留为实验，P0-3继续标记`partial`。

若后续发现V2.3.1在新增数据上质量不达标，可再启动判断段规则补齐（最多1～2轮，从v0.6继续，重点仍是原子化、任选关系、类别和熟练度；职责侧不做同步大改）。
