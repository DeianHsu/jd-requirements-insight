# 评审日志（Review Log）

## 最近一轮：报告改为求职准备清单（2026-09-21）

- 报告正文改为两张完整榜单：出现频率按独立 JD 数降序；准备优先级按明确必需篇数、
  加分篇数、总出现篇数依次降序，同分按名称排序。保留中文重要性计数、占比和来源编号。
- 移除正文中的指纹、模型版本、批次和实例身份；来源与原文证据放在末尾折叠区。
  任选条件从数据库实例传递并显式标注，避免被理解为每个选项都必须掌握。
- 历史来源风险仍披露，详细绑定说明折叠展示；报告生成门禁和数据库写入行为未改变。
- 更新 README、当前状态、架构及公开合成样例。已有私人报告需要显式指定数据库和批次
  离线重新生成；本轮没有调用付费模型或修改私人 JD、正式数据库。
- 验证：全量 344 个测试通过，Ruff 通过。测试覆盖频率/优先级差异、任选字段传递、
  确定性输出、CLI 离线生成、门禁失败不覆盖报告及已有全链路回归。
- 环境：uv 在沙箱内遇到缓存权限问题，沙箱外 pytest 启动器报 trampoline 路径错误；
  使用项目 .venv 的 python -m pytest 完成全量验证。uv Ruff 通过。
- 本轮按一个内聚的报告呈现模块提交；用户已有未跟踪 .idea/ 保持原状。
