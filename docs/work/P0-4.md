# P0-4 跨JD原子要求归并与映射

updated_at: 2026-08-02
current_revision: 875f300 + 未提交工作树改动（分阶段/分块输出边界与关系轮专用提示）
status: BLOCKED
next_action: 用户裁决 734/735 标注口径与验收策略（CR-P04-001），裁决前不继续付费调用

## 1. 目标与边界

### 目标

把选定 JD 范围内的全部要求实例动态归并到可统计的标准要求项，保存同义映射、组成/相关/上下位关系、来源证据和模型依据，并使每个结果批次可复现。

### 背景

纯字符串规则无法处理开放领域同义和关系，代码预置技能词表又不能扩展到新职业。LLM 负责提出语义归并，确定性代码负责完整性、冲突、身份和审计（`docs/DECISIONS.md` DEC-009、DEC-010）。

### 非目标

- 不修改 P0-1 `RequirementItem` 字段或预置 Python 等具体技能。
- 不建设独立发布版本系统，不允许失败时留下部分正式批次。
- 不为通过模型输出而放松完整覆盖、全局唯一或关系图合同。

### 验收标准

1. 149 条实例完整映射且 0 失败，任一失败不留下部分批次。
2. 13 实例人工集映射准确率不低于 84.62%；关系 Precision、Recall、F1 均不低于 80%；4 个关键人工边界正确。
3. 批次身份、幂等、输入变化、外键、全局唯一、关系互斥/无环和显式批次评测合同全部通过。
4. 定向测试、全量测试和 Ruff 通过；详细结果保存到 `reports/P0-4/`。

## 2. 接口与不变量

- **输入范围**：选定范围内覆盖全部 JD 的同一抽取器版本及其全部要求实例；批次身份 = 范围键 + 归并器版本（`model + prompt + schema`）+ 归并输入指纹（SHA-256）。输入变化不得复用旧批次。
- 相同输入重复执行不调用模型；输入或抽取器版本变化产生新批次。
- 所有输入实例恰好获得映射；标准项规范化名称全局唯一；关系类型互斥且图无环；任一阶段失败不写入批次（内存合成完整结果后统一校验、一次性持久化）。
- 内部模型调用允许分阶段或分块，但外部批次身份、完整覆盖、全局校验和原子持久化合同不得放松。

## 3. 当前实现

- `app/consolidation.py`：三阶段输出边界（标准项生成 → 按 `mapping_chunk_size` 分块映射 → 关系生成），各阶段独立有限重试、映射块块级覆盖校验；合成后统一执行合同、名称唯一、引用完整、关系互斥/无环与完整覆盖校验。149 实例映射轮拆为 50+50+49。
- 关系轮使用专用系统提示 `RELATION_SYSTEM_PROMPT`（聚焦关系判定、part_of 优先与跨层级限制）；标准项与映射轮共用 `CONSOLIDATION_SYSTEM_PROMPT`。Prompt 版本 v1.8 → v1.9 → v2.0 → v2.1。
- `app/consolidation_client.py`：解析容错（`candidate_requirement_ids: null` 规范化为空列表）。
- `scripts/experiments/p0_4/run_small_scale_precheck.py`：75 实例小规模预检入口。
- 已解决的历史问题：SQLite 外键未启用导致孤立记录风险（已开启并验证 0 违规）、输入身份不完整导致旧结果误复用（输入指纹）、关系 Precision/Recall/F1 评测与关系图冲突检测、指定批次离线评测 CLI（`evaluate-consolidation`）。

## 4. 当前验证

| 验证项 | 命令或数据范围 | 结果 | 证据 |
|---|---|---|---|
| 失败测试先行 | `tests/test_consolidation_chunking.py` | 复现旧实现约 50KB 截断失败（`Extra data ... char 45135`）后转绿；149 实例无单次超过 50 的映射请求 | 工作树测试 |
| 全量测试 | `python -m pytest --basetemp=.pytest-tmp -q`（.venv Python 3.11.15、pytest 9.1.1） | 120 passed | 工作树验证记录 |
| 静态检查 | `python -m ruff check app tests`（ruff 0.15.22） | All checks passed | 同上 |
| 75 实例预检 v1.8 | deepseek-v4-flash、抽取版本 2.3.1、chunk_size=10、max_attempts=3 | 映射 12/13=92.31%；关系 P/R/F1=66.67%/80%/72.73%；子图错误：rag~大模型应用开发多余、微调~落地判 related_to（应为 part_of） | `reports/P0-4/precheck-2.1.json`（含各轮请求记录，脱敏） |
| 75 实例预检 v1.9 | 同上 | 映射 11/13=84.62%；关系 0%/0%/0%（规则过保守，期望关系全部未输出） | 同上 |
| 75 实例预检 v2.0 | 同上 | 映射 13/13=100%；关系 66.67%/80%/72.73%；与 v1.8 相同两条错误（规则修正无效） | 同上 |
| 75 实例预检 v2.1 | 同上 | 映射 13/13=100%；关系 66.67%/80%/72.73%；rag~应用开发改判 part_of（标注期望无关系）；微调~落地仍 related_to | 同上 |
| 4 个关键人工边界 | 75 实例标注 | 667↔701 同义、672↔703 同义、656 与 730 不归并均正确；**734 part_of 735 全部 4 轮判为 related_to（未满足）** | 同上 |
| 历史批次 5 离线复算 | `evaluate-consolidation` 指定批次 | 映射 84.62%（11/13）、关系 P/R/F1=66.67%/80%/72.73% | `reports/P0-4/consolidation-prompt-analysis.md` |

## 5. 当前问题

| 问题 | 严重度 | 影响 | 下一步 |
|---|---|---|---|
| 关系质量未达门槛（CR-P04-001，open） | 阻塞 | 模型对“734 大模型微调经验 part_of 735 大模型应用落地经验”稳定判 related_to（4 轮一致，历史 v1.0~v1.4 报告已记载该分歧）；对“rag ~ 大模型应用开发”稳定输出关系（类型在 related_to/part_of 间波动，标注期望无关系）。验收标准 2 无法满足 | 用户裁决：a) 维持标注与门槛换更强模型重跑预检；b) 接受现状、149 正式验收仅获取真实指标并如实报告未达标；c) 调整标注口径或验收策略（需方案变更）；d) 继续 Prompt/few-shot（4 轮无收敛证据，成本高）。裁决前不继续付费调用 |
| 全图关系过度生成风险 | 中 | 全图关系数量 56~102 条波动大，任选组语言等并列项被全连接 | 正式验收时记录全图关系数量与错误分析；可设报告级警戒指标 |
| 149 实例正式验收未启动 | 中 | 无正式完整批次，完整覆盖和业务指标无法证明 | 预检达标或用户裁决后授权执行；届时 CLI 需 `--max-attempts 3` 并记录阶段请求规模 |

## 6. 关键变更

- 原计划：149 实例单次完整模型请求，对同一完整请求有限重试。
- 发现的问题：连续 3 次正式验收未形成批次，最后一次为约 50KB JSON 截断（单次请求超 20 实例时输出可达 15KB 以上，接近截断线）。
- 当前决定：改为分阶段/受控分块输出边界（标准项 → 分块映射 → 关系），内存合成完整结果后统一全局校验、原子持久化；合成阶段冲突整批失败不追加重试。
- 影响范围：只改变内部模型输出边界，外部批次身份、完整覆盖、全局校验和原子持久化合同未放松；验收标准 2 的关系门槛在预检中仍未达到。

## 7. 当前结论

确定性能力（批次身份、输入指纹、外键、关系评测、图冲突、指定批次评测）已通过验收标准 3 的既有支持与测试；分阶段/分块输出边界已实现并修复截断问题。但验收标准 2 的关系指标未达到（P=66.67% < 80%），**149 实例正式验收未启动，当前不能作为 P0-6 的稳定数据源**。恢复条件：用户对 CR-P04-001 裁决或预检达标后获得授权。结论适用 revision：875f300 + 未提交工作树改动。

## 8. 参考

- `reports/P0-4/precheck-2.1.json`（脱敏预检指标）、`reports/P0-4/consolidation-prompt-analysis.md`（历史报告）
- `data/private/experiments/P0-4/`（原始结果与诊断，不入库）
- `docs/DECISIONS.md`：DEC-009（跨 JD 标准项动态归并）、DEC-010（批次身份绑定输入版本）、CASE-002（归并 JSON 截断）、CASE-005（SQLite 外键）
