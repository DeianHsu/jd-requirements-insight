# P0-5 分层评测与错误分析（已并入 P0-3 / P0-4 / P0-10）

updated_at: 2026-08-03
implementation_revision: 1ab4bd0（并入前历史实现）
status: 已并入（不再作为独立开发阶段，DEC-018）

## 职责并入

P0-5 不再作为独立开发阶段维护。其职责按报告审核结论分别并入：

- **P0-3 抽取质量验证**：抽取合同、证据、变形性质与多次运行稳定性（Track A/B、pilot/acceptance 分层、人工规则审计）；
- **P0-4 归并质量验证**：P0-4A 事实归并合同与稳定性指标（co-clustering、同簇对 Jaccard、合并 P/R/F1、singleton/canonical 漂移、邻居稳定性、Top-cluster 成员集合稳定性）；P0-4B 为实验功能；
- **P0-10 报告质量摘要**：最终市场报告中展示质量摘要（P0-10 启动后落实）。

## 当前状态

旧评测能力（人工标准答案 Gold/F1、`evaluate-cases`/`evaluate-extractions`/`validate-golden` CLI、`app/evaluation.py` 的旧评测函数）已随 DEC-015/DEC-017 移除，历史由 Git 保存。`app/evaluation.py` 只保留 `item_label`/`item_name_similarity`（供抽取验证的确定性名称相似度 diagnostic 使用）。

## 参考

- `docs/DECISIONS.md`：DEC-008（分层评测思想并入验收分级）、DEC-015、DEC-017、DEC-018
