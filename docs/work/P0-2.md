# P0-2 JD结构化抽取

updated_at: 2026-08-03
implementation_revision: 1ab4bd0
status: APPROVED
next_action: 无；两段式 v0.6 作为正式抽取版本持续使用（历史 V2.3.1 结果保留，可用 evaluate-cases --prompt-version 2.3.1 复现）

## 1. 目标与边界

### 目标

把已导入 JD 按 P0-1 数据合同转换为可持久化、可追溯、可评测的结构化抽取结果，对结构或证据错误进行有限重试和明确记录。

### 非目标

- 不在本功能内优化原子粒度指标或跨 JD 归并。
- 不开发复杂前端、业务 API 或自动爬取。

### 验收标准

1. 真实 JD 可按统一数据合同完成模型抽取并持久化。
2. 结构错误、JSON 错误和证据不存在会触发有限重试，最终失败有明确原因。
3. 模型、Prompt、Schema 和抽取结果具有可追溯版本信息，版本间互不覆盖。
4. 小规模真实验证最终失败为 0，关键确定性路径有自动化测试。

## 2. 接口与不变量

- 使用 P0-1 正式数据合同，不在抽取阶段生成标准要求项；输出证据必须能在对应 JD 原文中定位。
- **抽取器版本** = `model_name + prompt_version + schema_version`，是抽取结果的身份；重跑不得覆盖其他版本结果。
- 当前正式版本：两段式 **v0.6** + 抽取数据合同 V2 + DeepSeek V4 Flash（`temperature=0`、JSON Object 模式）；正式链路经 P0-3 验收 APPROVED 后由 Prompt V2.3.1 切换（`docs/DECISIONS.md` DEC-014），V2.3.1 作为历史版本保留。
- 有费用调用必须显式执行。

## 3. 当前实现

- `app/extraction.py`：确定性请求配置、JSON 解析、合同校验、证据校验和有限重试；`extract_job` 转发两段式流程（`app/extraction_two_stage.py` 的发现段+判断段），`PROMPT_VERSION=0.6` 与 `TWO_STAGE_PROMPT_VERSION` 同步（测试锁定）；历史 V2.3.1 的 `SYSTEM_PROMPT` 保留供实验对比。
- `app/extraction.py`、`app/cli.py`：选择 JD、持久化成功结果、显示有限失败信息。
- `app/config.py`：LLM 配置。

## 4. 当前验证

| 验证项 | 命令或数据范围 | 结果 | 证据 |
|---|---|---|---|
| 正式版本真实验证 | 两段式 v0.6 + Schema V2，5 份真实 JD | 3 次独立验收运行均 0 最终失败（validation 端到端原子要求 F1 96.30%/71.43%/96.00% 全部 ≥70%） | 验收结论见 `docs/work/P0-3.md` 与 `reports/P0-3/validation-acceptance.md`（本地私有）；历史 V2.3.1 验收结论 APPROVED @ bdd8932 |
| 验收（1ab4bd0） | 正式版本切换时点全量测试 | 135 passed、Ruff 通过；覆盖两段式成功/重试/批量路径、版本号一致性与历史 Prompt 锁定 | Git 记录 `1ab4bd0` |

## 5. 当前问题

| 问题 | 严重度 | 影响 | 下一步 |
|---|---|---|---|
| 已知粒度弱点（jd_003 端到端交付边界、上位单条列举） | 中 | case_020/case_021 在 3 次验收运行中全部失败，但不影响正式门槛 | 列入回归关注点，v0.7 或后续迭代处理 |

## 6. 关键变更

- 原子化策略：先识别独立交付再决定合并动作、区分非穷举示例与直接点名技术（`docs/DECISIONS.md` DEC-007），当前正式版本 V2.3.1 已实现该语义边界。

## 7. 当前结论

正式可用。正式版本已由 Prompt V2.3.1 切换为两段式 v0.6（P0-3 验收 APPROVED，revision 1ab4bd0），仍是 P0-3、P0-4、P0-8 的稳定上游；版本可追溯与幂等键机制不变，V2.3.1 历史结果保留。

## 8. 参考

- `docs/GLOSSARY.md`：JD、抽取器版本、证据
- `docs/DECISIONS.md`：DEC-005、DEC-007
