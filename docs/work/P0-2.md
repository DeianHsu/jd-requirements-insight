# P0-2 JD结构化抽取

updated_at: 2026-08-02
current_revision: bdd8932（验收基线；工作树无本功能相关改动）
status: APPROVED
next_action: 无；Prompt V2.3.1 作为正式抽取版本持续使用

## 1. 目标与边界

### 目标

把已导入 JD 按 P0-1 数据合同转换为可持久化、可追溯、可评测的结构化抽取结果，对结构或证据错误进行有限重试和明确记录。

### 非目标

- 不在本功能内优化原子粒度指标或跨 JD 归并。
- 不开发复杂前端、业务 API 或自动爬取。

### 验收标准

1. 真实 JD 可按统一数据合同完成模型抽取并持久化。
2. 结构错误、JSON 错误和证据不存在会触发有限重试，最终失败有明确原因。
3. 模型、Prompt、Schema 和抽取结果具有可追溯版本信息，版本间互不覆盖。
4. 小规模真实验证最终失败为 0，关键确定性路径有自动化测试。

## 2. 接口与不变量

- 使用 P0-1 正式数据合同，不在抽取阶段生成标准要求项；输出证据必须能在对应 JD 原文中定位。
- **抽取器版本** = `model_name + prompt_version + schema_version`，是抽取结果的身份；重跑不得覆盖其他版本结果。
- 当前正式版本：Prompt **V2.3.1** + 抽取数据合同 V2 + DeepSeek V4 Flash（`temperature=0`、JSON Object 模式）。
- 有费用调用必须显式执行。

## 3. 当前实现

- `app/extraction.py`：确定性请求配置、JSON 解析、合同校验、证据校验和有限重试。
- `app/services.py`、`app/cli.py`：选择 JD、持久化成功结果、显示有限失败信息。
- `app/config.py`：LLM 配置。
- 两段式 v0.6 候选位于 `app/extraction_two_stage.py`，未接入正式流程。

## 4. 当前验证

| 验证项 | 命令或数据范围 | 结果 | 证据 |
|---|---|---|---|
| 正式版本真实验证 | Prompt V2.3.1 + Schema V2，5 份真实 JD | 最终失败 0 份 | 验收结论 APPROVED @ bdd8932（本节第 7 部分；历史裁决全文由 Git 保存） |
| 验收（bdd8932） | 验收时点全量测试 | 111 passed、Ruff 通过；覆盖重试、证据校验和版本隔离 | 同上 |
| 当前工作树回归 | `pytest --basetemp=.pytest-tmp -q`、`ruff check app tests` | 120 passed、Ruff 通过 | 工作树验证记录（P0-9 文档） |

## 5. 当前问题

| 问题 | 严重度 | 影响 | 下一步 |
|---|---|---|---|
| 原子粒度不足 | 中 | 正式版本要求/职责粒度指标低于候选门槛 | 属于 P0-3 候选（v0.6）独立验收，通过后才替换 V2.3.1 |

## 6. 关键变更

- 原子化策略：先识别独立交付再决定合并动作、区分非穷举示例与直接点名技术（`docs/DECISIONS.md` CASE-001），当前正式版本 V2.3.1 已实现该语义边界。

## 7. 当前结论

正式可用。验收结论 APPROVED（revision bdd8932），是 P0-3、P0-4、P0-8 的稳定上游；后续 Prompt 候选只有在独立验收通过后才替换 V2.3.1。

## 8. 参考

- `docs/GLOSSARY.md`：JD、抽取器版本、证据
- `docs/DECISIONS.md`：DEC-005、DEC-007
