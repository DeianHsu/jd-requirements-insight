# P0-4 跨JD原子要求归并与映射｜Review

```yaml
p0_id: "P0-4"
verdict: "CHANGES_REQUESTED"
task_commit: "1312452"
reviewed_revision: "bdd8932"
reviewed_at: "2026-08-02"
```

## 验收结论

`CHANGES_REQUESTED`。从`bdd8932`到当前`1312452`没有P0-4业务代码或测试实现变化。当前代码仍把约149条实例放入一次完整模型请求，并只对同一完整请求进行有限重试，没有实现当前Design要求的分阶段或分块输出边界。既有3次正式验收均未生成可评测批次，也没有新正式指标，因此P0-4不得标记完成、不得作为P0-6稳定输入，并继续暂停修复和付费模型调用。

## 验收标准逐项检查

| 验收标准 | 结果 | 依据 |
|---|---|---|
| 149实例完整映射、0失败且无部分批次 | 未通过 | 3次正式验收均未形成批次；当前仍是单次完整请求，第3次为约50KB JSON截断 |
| 映射≥84.62%，关系P/R/F1均≥80%，4个边界正确 | 未验证 | v1.7只有75实例预检，无149实例正式批次可评测 |
| 批次身份、幂等、范围、外键和关系合同 | 有既有支持 | 代码与测试源码覆盖输入指纹、完整范围、幂等、失败不持久化、关系覆盖及图合同；Developer未提供本轮绑定revision的完整执行报告 |
| 实现分阶段或分块输出并保持全局校验、原子持久化 | 未通过 | `app/consolidation.py`的`consolidate_with_correction()`每轮调用一次完整Prompt；`consolidate_requirements()`仍明确“不做分批” |
| 定向测试、全量测试和Ruff通过 | 证据不足 | Develop只有111项与定向28项摘要，缺少实际命令、环境、输出报告和revision绑定 |

## 当前已确认问题

| 问题ID | 严重度 | 问题 | 代码或实验依据 |
|---|---|---|---|
| P04-ISSUE-001 | 高 | 当前单次完整响应边界无法可靠承载149实例，且尚未实施Design已接受的分阶段/分块方案 | `app/consolidation.py:399`附近及`consolidate_requirements()`；P0-4实验报告中的3次正式失败 |
| P04-ISSUE-002 | 高 | 缺少149实例正式成功批次，完整覆盖和业务指标均无法证明 | `reports/P0-4/consolidation-prompt-analysis.md` |
| P04-ISSUE-003 | 中 | 代码文档字符串仍声称约150条实例“已验证可单次调用完成，不做分批”，与连续3次正式失败及当前Design直接冲突 | `app/consolidation.py:537`附近 |
| P04-ISSUE-004 | 中 | v1.7小规模全图关系为102条，存在过度生成风险，尚无完整批次错误分析 | P0-4实验报告v1.7预检摘要 |
| P04-ISSUE-005 | 中 | 当前测试摘要不满足命令、环境、revision和详细报告要求 | `docs/develop/P0-4.md`“当前测试结果” |

## 复现方法

- Reviewer通过只读代码和Git差异确认当前实现仍是完整单请求；未执行静态检查、测试、离线评测或模型实验。
- Developer提供的无费用复现方式应覆盖归并定向测试、完整测试和Ruff，并记录实现revision、实际命令、环境和输出报告。
- 付费复现仍未获授权；只有完成新输出边界和无费用证据后，用户才能另行决定是否授权75实例预检与149实例正式验收。

## 必须修改项

1. 对应 `P04-ISSUE-001`：用失败测试驱动实现可控的分阶段或分块输出边界，避免单次约50KB响应截断；不得仅增加超时、重试次数或继续堆叠单体Prompt规则。
2. 对应 `P04-ISSUE-001`、`P04-ISSUE-002`：所有阶段必须先在内存合成为完整结果，再统一执行完整覆盖、名称唯一、关系互斥/无环和来源校验；任一阶段失败不得写入正式批次。
3. 对应 `P04-ISSUE-003`：更新与实现冲突的代码文档字符串和测试名称，删除“已验证单次完整调用”的错误事实，并增加新输出边界、跨块全局冲突及失败不持久化测试。
4. 对应 `P04-ISSUE-005`：Developer补充与新实现revision绑定的定向测试、全量测试和Ruff证据，包括命令、环境、结果、警告和详细报告路径。
5. 对应 `P04-ISSUE-004`：完成无费用实现与测试后再请求用户授权小规模预检；记录响应大小、阶段数量、全图关系数量和映射/关系指标。
6. 对应 `P04-ISSUE-002`：小规模达到既有门槛后，另行获得授权完成149实例正式验收；只有Design全部固定指标达到后才重新申请Review。

## Tech Lead直接处理的治理事项

- 本轮未发现需要Tech Lead直接修改的独立治理材料；`P04-ISSUE-003`位于业务代码文档字符串中，应随Developer实现与测试一起修正。

## 可选优化项

- 在不改变业务批次合同的前提下记录每阶段耗时、输入输出数量和响应大小，用于评估P0-8扩充后的边界。
- 对全图关系数量设置报告级警戒指标，但不要未经Design变更直接增加影响业务语义的硬阈值。

## Change Request处理

- 继续接受Developer关于改变内部模型输出边界的建议：当前Design允许分阶段或分块，但外部批次身份、完整覆盖、全局校验和原子持久化合同不得放松。
- 继续拒绝通过减少输入范围、允许部分映射、删除关系合同或隐式选择局部JD来绕过响应截断。

## 最终状态

- P0-4保持`CHANGES_REQUESTED`、`blocked`和付费调用暂停；P0-6硬依赖不得解除。本次“验收”请求不构成恢复开发或付费调用授权。
