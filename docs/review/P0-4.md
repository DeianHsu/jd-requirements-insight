# P0-4 跨JD原子要求归并与映射｜Review

```yaml
p0_id: "P0-4"
verdict: "CHANGES_REQUESTED"
task_commit: "historical; normalized at bdd8932"
reviewed_revision: "bdd8932"
reviewed_at: "2026-08-02"
```

## 验收结论

`CHANGES_REQUESTED`。确定性批次合同和小规模Prompt指标已有实质进展，但`bdd8932`对应实现连续3次未能生成149实例正式批次，无法证明完整覆盖、正式业务指标或下游可用性。P0-4不得标记完成，也不得解除P0-6硬依赖。

## 验收标准逐项检查

| 验收标准 | 结果 | 依据 |
|---|---|---|
| 149实例完整映射、0失败且无部分批次 | 未通过 | 3次正式验收均未形成批次，第3次为约50KB JSON截断 |
| 映射≥84.62%，关系P/R/F1均≥80%，4个边界正确 | 未验证 | v1.7仅有75实例预检；无149实例正式批次可评测 |
| 批次身份、幂等、范围、外键和关系合同 | 通过 | 当前确定性实现、定向测试及数据库副本检查 |
| 定向、全量测试与Ruff通过 | 通过 | 当前111项测试、v1.7相关定向28项及Ruff通过记录 |

## 当前已确认问题

| 严重度 | 问题 | 代码或实验依据 |
|---|---|---|
| 高 | 单次大响应无法可靠完成149实例归并，连续3次正式验收失败 | `reports/P0-4/consolidation-prompt-analysis.md` |
| 中 | v1.7小规模全图关系增至102条，存在关系过度生成风险 | 同报告中的v1.7预检摘要 |

## 复现方法

- Developer提供的无费用复现方式：运行归并相关定向测试、全套`pytest`和`ruff check .`，确认合同与持久化行为；Reviewer不代为执行。
- Developer付费复现方式：当前禁止自动执行；用户明确授权恢复后，由Developer先运行75实例预检，再决定是否运行149实例正式验收。

## 必须修改项

1. 用失败测试驱动实现可控的分阶段或分块输出边界，避免单次约50KB响应截断。
2. 保证所有阶段在内存合成为完整结果后统一执行覆盖、唯一、关系互斥/无环校验，任一失败不写入正式批次。
3. 用户授权后先通过75实例预检并记录响应大小、关系数量及映射/关系指标。
4. 重新完成149实例正式验收，达到Design全部固定指标后再申请Review。

## 可选优化项

- 在不改变业务合同的前提下记录分阶段耗时与响应大小，便于判断后续扩展边界。

## Change Request处理

- **接受**Developer关于改变内部模型输出边界的建议：当前Design已允许分阶段或分块，但外部批次身份、完整覆盖、全局校验和原子持久化合同不得放松。
- **不接受**以降低覆盖范围或删除关系合同来绕过响应截断。

## 最终状态

- 保持`CHANGES_REQUESTED`和付费调用暂停；只有用户重新授权后才开始新的正式验收计数周期。
