# JD Requirements Insight 仓库约束

## 当前唯一主线

```text
JD 导入
→ v0.10 + Schema V3 抽取 acceptance（多次运行 + 合同检查）
→ 人工审核 → finalize-extraction
→ requirement instance 归并 acceptance（多次运行 + 稳定性分析）
→ 人工裁决 → finalize-consolidation
→ 独立 JD 统计
→ 原文证据追溯
→ Markdown 市场分析报告
```

`extract-jds --candidate-output` 与
`consolidate-requirements --candidate-output` 只生成可选的单次私有预检，
不是正式链必经步骤，产物
不作为 finalize 输入，也不得写入正式抽取/归并表。

v0.1 已完成并冻结。默认只维护当前闭环、修复明确缺陷并保持可复现性；
未经用户明确授权，不扩充样本、不新增功能、不进入下一实施阶段。

不维护已淘汰、冻结或不服务当前 MVP 的功能；不维护旧方案与历史兼容
（旧抽取版本、旧 Schema、旧数据库结构、层级关系）。历史由 Git 保存。

## 新会话推荐阅读顺序

```text
AGENTS.md
→ README.md
→ docs/CURRENT_STATE.md
→ docs/ARCHITECTURE.md
→ 根据任务读取 GLOSSARY、annotation 或对应代码
```

涉及完整项目检查、生产验收、模块关闭、正式数据发布时，必须按
AGENTS.md「生产验收协议」小节选择审计等级执行（普通提交检查不触发）。

## 事实优先级

1. 当前代码、测试、数据结构和 Git 状态；
2. `docs/CURRENT_STATE.md` 的当前项目事实；
3. 聊天记忆只作辅助，不得覆盖正式文件。

## 变更约束

1. 不得静默改变公共接口、Schema、数据语义、功能范围；重要变化更新
   对应文档（CURRENT_STATE / ARCHITECTURE / GLOSSARY / annotation）。
2. 不得覆盖、删除或重置用户已有改动；提交时使用路径级暂存。
3. 真实 JD、数据库、密钥和原始模型响应不得提交；私有材料只存在于
   `data/private/`、`data/raw_jds/` 与本地数据库。
4. 文档只保存当前有效事实，不记录迭代过程；不新增 DEC/历史归档来
   记录清理，不用 deprecated/legacy/experimental 标记代替删除。

## v0.1 冻结维护原则

1. v0.1 MVP 闭环已经完成；当前默认目标是保持其正确、可演示、可评测和
   可复现，不把维护任务解释为继续建设产品。
2. 默认采用满足当前需求的最小实现，不提前建设通用框架。
3. 一次任务可以处理一组紧密相关的问题，但不得扩展到无直接关系的邻近问题。
4. 只有真实数据、现有测试或明确需求证明必要时，才新增抽象、兼容逻辑、配置、脚本或验证规则。
5. 优先复用、简化和删除，不为未来可能需求增加当前复杂度。
6. 测试只覆盖核心合同、真实缺陷和关键回归，不做排列组合式穷举。
7. 文档只记录当前事实、关键边界和下一步，历史过程交给 Git。

### 相邻问题处理原则

- 与当前修改直接相关、风险明确、成本较低的问题，应顺手修复并验证；
- 只有在问题与当前目标无关、需要扩大架构或明显增加工作量时，才只记录不展开；
- 不得以“轻量化”为理由保留明显错误，也不得借“顺手修复”扩大任务边界；
- 修改范围明显超出原任务时，停止扩展并回到最小闭环。

不削弱安全、隐私、版本门禁和付费调用保护规则。

## 验证与实验

1. 修改后运行：`uv run pytest`（系统 Temp 被锁定时加 `--basetemp .pytest-tmp`）
   与 `uv run ruff check app scripts tests`；
2. 自动化测试不得调用付费外部服务；测试使用临时文件和临时数据库；
3. 付费模型调用必须显式 `--execute` 确认（extract-jds /
   consolidate-requirements / 验证脚本均如此），并先说明模型、数据范围
   和目标；数据库操作必须显式选择目标，不得隐式写入项目数据库；
4. 遇到旧抽取数据或旧数据库结构：不兼容、不迁移、不自动删除，给出
   提示"备份原始 JD，删除旧派生数据库并重新生成"。

## 评审日志（Review Log）

- 每次任务完成时，将**给用户的简短总结回复**（任务做了什么、核实结论、
  执行提交、验证结果、当前状态）**覆盖更新**到 `docs/REVIEW_LOG.md`——
  文档只保留最后一次任务的简短总结，不保留历史（历史由 Git 保存）；
- 定位：供外部 Reviewer（如 ChatGPT）读取的最近一轮执行摘要；外部评审
  文本由 Reviewer 自己持有，不复制评审全文；
- Review Log 不记录提交是否已推送；远程状态以 Git/GitHub 为准；
- 覆盖更新随任务提交一起 commit，对话收尾 push 时一并推送。

## 生产验收协议（Production Audit）

完整项目检查、生产主线验收、模块关闭、正式数据发布时，必须按本节执行；
普通提交检查、单点修复、测试排查、小范围 review 走局部检查，不触发。

### 审计等级（按检查对象选择，不看关键词）

- **L1 提交检查**：普通提交、局部修复、单点 bug、测试排查。diff review +
  相关测试 + 局部合同核对，不做全链路追踪；
- **L2 模块关闭验收**（单个模块默认 L2）：artifact tracing（正常路径 +
  关键失败路径）、测试真实性、文档同步；
- **L3 生产主线审计**：正式生产主线、跨模块架构变更、正式数据发布。
  完整端到端、上下游 provenance、数据库副作用、批量/单条兼容、幂等与
  事务、代表性反例、真实产物验证。

L2 升级 L3 的条件（任一）：跨模块 artifact 传递；正式数据库写入；
provenance 或审核身份；数据迁移；付费模型结果正式化；对外报告或发布。

### 核心规则

1. **不信任声明**：文档、提交信息、测试名称中的“已完成/闭环/达标”一律为
   待验证声明，必须用代码和实际执行重新证明；实现存在 ≠ 流程通，
   测试绿 ≠ 正式入口被调用；
2. **artifact tracing**：选代表性产物（正常/批量/异常各一，不穷举）沿
   正式路径逐站追踪：上一步输出是否直接是下一步输入（找不到消费方 =
   断点）、是否需手工改中间 JSON、是否重复付费调用、身份字段是否无损
   传递、批量/单条是否兼容、失败是否留正式数据；
3. **测试真实性**：关键 E2E 是否调用正式入口？是否手工构造
   report/raw/审核文件？（夹具手工构造合理；声称证明正式闭环却绕过
   真实入口 = 问题）；
4. **门禁上下游**：每个安全门检查上游覆盖——归并定稿是否验证上游抽取
   provenance？报告是否验证归并完整定稿字段？“本层已定稿但上游未绑定”
   必须显式报告；
5. **反例预算**：3~5 个最高风险反例（身份混用、范围不一致、部分失败、
   重复执行、上游缺失）；发现 blocker 后停止穷举，记录剩余范围；
6. **证据等级**：blocker 必须附代码（file:line）或运行证据；非阻塞项至少
   一个定位依据；无数据支撑的预测一律标注“假设/待验证”，不得作为方案
   依据；
7. **红旗清单（调查信号，不自动判失败）**：文档出现“合成/模拟/手工”；
   测试手工拼中间产物；产物只有写方没有读方；unverified 上游 +
   reportable 下游（须存在结构化 legacy waiver：被豁免记录、原因、证据、
   批准人、允许消费的下游、是否仅限当前批次、新增数据禁止继续豁免；
   普通状态说明不构成豁免，仍需报告风险）；批量/单条不兼容；门禁不查
   上游。

### 输出与边界

- 结论四层：当前基线 blocker / 下一阶段 blocker / 长期规模化风险 /
  非阻塞卫生项；附最小修复顺序（默认不改代码，除非用户授权）；
- 停止条件：出现 P0 blocker 后只查同一链路直接影响，记录剩余范围；
  全部满足才算结束（每站有生产者和消费者、代表性正常路径通过、最高
  风险失败路径有门禁、关键 E2E 未绕过正式接口、结论均有证据等级）；
- 默认只读正式数据；允许临时目录/临时数据库验证；不调用付费模型。

## Git 提交与推送

- 可以自行 commit：只暂存本任务相关文件，填写清晰的 Summary/Description
  （修改内容、原因、验证、剩余风险），每次提交内部结构一致；
- **Summary 用中文**（保留 Conventional Commits 的 type/scope 英文
  关键词，如 `fix(p0-4): 完成离线收口`），Description 用中文；
- **按完整小模块提交**：一个内聚的、可独立验证的改动单元（如一次
  安全门修复、一个分析脚本）完成后才提交，不零零散散、改一点点就
  提交；模块中途的中间状态保留在工作区，直到该模块完成并通过验证
  后一次提交；
- **一次对话**按“一次用户请求 → 助手完成当前任务并给出最终回复”的一次
  来回计算，不按整个聊天线程计算；
- 当前任务执行期间不 push；任务完成、准备给出最终回复时，将这次来回产生的
  所有 commit 一次性推送到远程（`git push`），每次来回最多 push 一次，
  推送前在回复中说明将推送的提交数与范围；
- 不得改写已有 Git 历史、amend 用户提交或删除用户提交；push 遇到
  非 fast-forward（远程已有新提交）时停止并报告，不 force push、
  不 rebase 用户提交。
