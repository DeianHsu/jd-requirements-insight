# JD Skill Insight Codex开发行为规则

本文件适用于当前目录及子目录，只规定Codex开发行为。

## 规则优先级

1. 在本项目内，`AGENTS.md`定义最高优先级的Codex开发行为。
2. `docs/CONTEXT_ROUTING.md`只选择上下文，不得覆盖本文件。
3. 其他文档按各自职责提供详情，本文件不重复；参见`docs/DOCUMENT_RULES.md`、`docs/PROJECT_PLAN.md`和`docs/DECISIONS.md`。

## 上下文读取

1. 任务开始先读取本地`docs/CONTEXT_ROUTING.md`（如存在），选择最小必要上下文。
2. 禁止默认读取全部项目文档、完整真实JD、完整模型JSON或整个标注文件。
3. 长文档先搜索标题和关键词，代码先定位类、函数或常量，再读目标片段。
4. 避免批量读取文档；扩大范围前，先确认是否有更小范围的替代信息。

## 代码与中文注释

1. 代码目录`README.md`按`docs/DOCUMENT_RULES.md`维护；代码文件增删或改名时同步。
2. 每个代码文件必须包含中文模块说明；公共类、函数和方法必须包含中文文档字符串，私有辅助函数根据复杂度和维护需要决定是否添加。
3. 注释重点解释职责和设计原因；不直观代码块按需添加中文注释，避免逐行翻译或无意义注释。
4. 修改行为时同步检查注释，不保留过时说明。

## 验证要求

1. 后续各环节按“静态检查→小规模验证→全量回归→版本确认”推进；前一阶段未通过，不进入下一阶段。
2. 静态检查先验证语法、类型、Schema、接口及确定性规则；功能代码运行相关自动化测试和Ruff，不调用有费用的外部服务。
3. 小规模验证只使用能覆盖本次变化的少量测试、case或数据；LLM、Prompt和业务语义使用少量真实样例。
4. 小规模验证通过后，才按影响范围执行完整测试和真实数据回归；昂贵的全量LLM调用只在此阶段进行。
5. 全量回归达到`docs/PROJECT_PLAN.md`已有验收标准后，才能确认版本或标记完成；未达到时保留为开发状态。
6. 纯文档或配置改动只检查内容、链接、格式、忽略规则和敏感信息，不做无关测试。

## 文档同步

1. 按`docs/DOCUMENT_RULES.md`维护唯一信息源；功能稳定后统一同步，不在多处复制完整内容。
2. 计划功能的范围、状态或验收结果变化时，同步`docs/PROJECT_PLAN.md`（如存在）。
3. 长期产品或技术选择写入`docs/DECISIONS.md`（如存在）；普通实现和每日进度不写入。
4. 执行GitHub发布相关任务时遵守`docs/PUBLISH_RULES.md`。

## 技术讲解

1. 技术讲解提供项目文件路径；可以定位时附关键类、函数或行号。
2. 重要技术点列出核心实现和测试文件，说明实现及验证位置。
3. 未实现的概念明确标注“尚未实现”，不得提供误导性路径。

## 完成前检查

确认README、中文说明、计划和决策已按需同步，验证符合风险且信息未重复；发布任务另按`docs/PUBLISH_RULES.md`检查。
