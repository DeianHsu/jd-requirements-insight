# JD Skill Insight Codex开发行为规则

本文件适用于当前目录及子目录，只规定Codex开发行为。

## 规则优先级

1. 在本项目内，`AGENTS.md`定义最高优先级的Codex开发行为。
2. `docs/CONTEXT_ROUTING.md`只选择上下文，不得覆盖本文件。
3. 其他文档按各自职责提供详情，本文件不重复；项目术语以`docs/GLOSSARY.md`为唯一信息源，其他规则参见`docs/DOCUMENT_RULES.md`、`docs/PROJECT_PLAN.md`和`docs/DECISIONS.md`。

## 新会话工作交接

1. 用户声明“当前开发P0-X”即构成有效的上下文路由指令，无需另行列出待读文档。
2. 开始工作前按`docs/handoff/README.md`第2节的启动顺序恢复上下文；`docs/GLOSSARY.md`按目标P0 handoff的`glossary_terms`定点读取对应章节及“跨阶段不变量”，不默认读取全文，术语语义存疑、发现同词异义或定义冲突时再读取全文。
3. 上游依赖读取、下游影响检查、handoff维护时机和内容结构统一遵守`docs/handoff/README.md`，本文件不重复规定。
4. 用户未说明具体任务时，只恢复上下文并汇报当前状态、依赖、阻塞项和建议下一步，不执行修改。
5. 避免默认读取全部P0 handoff、完整真实JD、完整标注数据、完整模型JSON或数据库内容；确有需要时按需读取。

## 上下文读取

1. 自行判断完成任务所需的最小上下文：能搜索定位就不全文读取，能用代码、Git或测试确认就不依赖文档；`docs/CONTEXT_ROUTING.md`提供常见任务的默认读取建议，可按实际任务调整。
2. 代码、测试和文档中的项目术语必须符合`docs/GLOSSARY.md`；出现新术语、同词异义或定义冲突时先更新词典，不得在单个功能中自行约定另一套含义。

## 代码与中文注释

1. 代码目录`README.md`按`docs/DOCUMENT_RULES.md`维护；代码文件增删或改名时同步。
2. 每个代码文件必须包含中文模块说明；公共类、函数和方法必须包含中文文档字符串，私有辅助函数根据复杂度和维护需要决定是否添加。
3. 注释重点解释职责和设计原因；不直观代码块按需添加中文注释，避免逐行翻译或无意义注释。
4. 修改行为时同步检查注释，不保留过时说明。

## 验证要求

1. 后续各环节按“静态检查→小规模验证→全量回归→版本确认”推进；前一阶段未通过，不进入下一阶段。
2. 静态检查先验证语法、类型、抽取数据合同、数据库结构、接口及确定性规则；功能代码运行相关自动化测试和Ruff，不调用有费用的外部服务。
3. 小规模验证只使用能覆盖本次变化的少量测试、困难样例或数据；LLM、Prompt和业务语义使用少量真实样例。
4. 小规模验证通过后，才按影响范围执行完整测试和真实数据回归；昂贵的全量LLM调用只在此阶段进行。
5. 全量回归达到`docs/PROJECT_PLAN.md`已有验收标准后，才能确认版本或标记完成；未达到时保留为开发状态。
6. 纯文档或配置改动只检查内容、链接、格式、忽略规则和敏感信息，不做无关测试。

## 文档同步

1. 按`docs/DOCUMENT_RULES.md`选择唯一信息源，功能稳定后统一同步，不在多处复制完整内容。
2. GitHub发布任务另按`docs/PUBLISH_RULES.md`检查。

## 提交权限

Codex只准备建议提交范围、Summary和Description；commit和push均由用户手动执行。

## 技术讲解

1. 技术讲解提供项目文件路径；可以定位时附关键类、函数或行号。
2. 重要技术点列出核心实现和测试文件，说明实现及验证位置。
3. 未实现的功能明确标注“尚未实现”，不得提供误导性路径。

## 完成前检查

确认README、中文说明、计划和决策已按`docs/DOCUMENT_RULES.md`同步，验证符合风险且信息未重复；对新增或改名的公共类、枚举、CLI、文件和标题执行术语搜索，确认符合`docs/GLOSSARY.md`且旧称只出现在明确标记的历史说明中；发布任务另按`docs/PUBLISH_RULES.md`检查。
