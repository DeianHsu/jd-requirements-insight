# JD Skill Insight 仓库约束

本文件适用于项目根目录及全部子目录，是进入仓库后必须先读的最高级通用规则。文档只记录项目本身：目标与路线、当前功能事实、核心业务术语、有面试价值的技术决策、重大技术问题及解决经验、当前有效的验证证据。

## 1. 事实优先级

发生冲突时按以下顺序判断，并修正文档而不是保留两套事实：

1. 当前代码、测试、数据结构和 Git 状态是实现事实。
2. 当前有效实验报告（`reports/P0-X/`，多数为本地私有材料）。
3. `docs/work/P0-X.md` 记录功能目标、当前状态、验证结果、问题和下一步。
4. 聊天记忆只作辅助，不得覆盖正式文件。

项目术语以 `docs/GLOSSARY.md` 为唯一信息源，长期技术决策以 `docs/DECISIONS.md` 为唯一信息源，P0 状态和功能硬依赖以 `docs/PROJECT_PLAN.md` 为唯一信息源，标注语义以 `docs/annotation/` 为唯一信息源。

## 2. 文档职责

- `docs/PROJECT_PLAN.md`：全局项目地图——定位、MVP 范围、P0 列表、功能硬依赖、每个 P0 的一句话验收目标、当前状态、阻塞或下一步。
- `docs/work/P0-X.md`：单个 P0 的当前事实——目标与边界、接口与不变量、当前实现、当前验证、当前问题、关键变更、当前结论。只保存当前有效事实，历史由 Git 保存。
- `docs/GLOSSARY.md`：容易导致业务语义漂移的核心术语，不复制代码结构或完整枚举。
- `docs/DECISIONS.md`：有面试价值的技术决策（DEC）和重大问题解决经验（CASE）。
- `docs/annotation/`：JD 职责、要求、证据、数据集与评测的标注规范。
- `reports/P0-X/`：详细实验材料，按 P0 分组，默认被 Git 忽略。
- 各 README：面向人类的项目说明、目录索引与运行方式。

修改前按任务定点读取相关代码、测试和文档，不默认读取整个仓库、完整数据库、全部报告或私有材料；切换任务时丢弃未写入正式文件的旧假设。

## 3. 变更约束

1. 不得静默改变公共接口、Schema、数据语义、功能范围和验收标准；出现重要方案变化时，直接更新对应 `docs/work/P0-X.md` 的“关键变更”。
2. 不得覆盖、删除或重置用户已有改动；提交时使用路径级暂存，只暂存本任务相关文件。
3. 真实 JD、人工标准答案、数据库、密钥、原始模型响应和私人材料不得提交；详细实验材料只放在被 Git 忽略的私有位置。
4. 文档只保存当前有效事实；过期内容从当前文档清理，历史由 Git 保存。
5. 纯文档任务只检查内容、路径、链接、格式、忽略规则和敏感信息，不运行无关业务测试或付费实验。

## 4. 验证与实验

按以下顺序推进验证，前一层未通过不进入下一层：

1. 静态检查（语法、数据合同、Ruff 等）；
2. 目标测试或小规模样例；
3. 影响范围内的完整回归；
4. 必要且获授权的正式实验；
5. 检查 Git diff 与验收标准。

其他规则：

- 自动化测试不得调用付费外部服务；测试使用临时文件和临时数据库。
- 付费模型调用必须显式触发（如 `--execute`），并先说明模型、数据范围和目标；数据库操作必须显式选择目标，不得隐式写入项目数据库。
- 实验脚本统一放在 `scripts/experiments/p0_x/`，只编排正式模块；可复用逻辑进入 `app/` 并配套测试，实验脚本不得成为未声明的正式入口。

## 5. Git 提交与推送

### 可以自行提交

Agent 应自行判断合理的提交时机，在形成完整、可验证、可独立回退的阶段后：

1. 检查 `git diff` 和 `git status`，确认改动范围；
2. 只暂存本任务相关文件，不把用户已有的无关改动放入提交；
3. 自行填写清晰的提交 Summary 和 Description：说明本次修改内容、修改原因、关键实现或文档变化、执行的验证、已知限制或剩余风险；
4. 执行 `git commit`。

不需要等待用户逐次批准 commit。提交粒度由 Agent 根据实际变更情况决定，每次提交必须内部结构一致，不提交半迁移状态。

### 严格禁止推送

```text
git push
git push --force
git push --force-with-lease
```

也不得：

- 创建或修改远端分支；
- 改写已经存在的 Git 历史（rebase、filter-repo 或 reset 丢弃用户内容）；
- amend 用户已有提交；
- 删除用户已有提交。

所有 push 均由用户手动执行。
